<%= form_with(model: quill) do |form| %>
  <% if quill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quill.errors.count, "error") %> prohibited this quill from being saved:</h2>

      <ul>
        <% quill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_area :content %>
  </div>

  <div>
    <%= form.label :project_id, style: "display: block" %>
    <%= form.text_field :project_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
