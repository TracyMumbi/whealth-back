<%= form_with(model: otp) do |form| %>
  <% if otp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(otp.errors.count, "error") %> prohibited this otp from being saved:</h2>

      <ul>
        <% otp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :otp_no, style: "display: block" %>
    <%= form.text_field :otp_no %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
